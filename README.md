# Hardware Monitor

The project connects to a microcontroller and gets debug info, logs,
sensor data and plots them in graphs based on the uer preference.

The app is useful when debugging data from a microcontroller.

## Installation